---
import "../../components/Styles.astro";
import Default from "../../layouts/Default.astro";

export async function getStaticPaths() {
	const posts = await Astro.glob("../../posts/**/*.mdx");
	console.log(posts);

	return [
		{
			params: {
				slug: "hello-world",
			},
		},
	];
}

const { Content } = Astro.props.post;
---

<Default>
	<Content />
</Default>
